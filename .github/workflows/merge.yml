name: AutoMerger

on:
  issue_comment:
    types: [created]

jobs:
  release_labeler:
    name: Auto Merger
    runs-on: ubuntu-latest
    steps:
      - uses: aaomidi/AutoMerge@master
        with:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
